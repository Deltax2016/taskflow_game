# --- Web-фреймворк и сервер ---
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- База данных (async SQLAlchemy + драйвер Postgres) ---
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0

# --- Валидация и настройки ---
pydantic==2.9.2
pydantic-settings==2.5.2

# --- HTTP-клиент для похода в LLM (OpenRouter) ---
httpx==0.27.2

# --- Оркестрация агента: граф состояний, retry policy, checkpoint, HITL ---
langgraph==1.2.10
# Чекпоинтер LangGraph поверх Postgres — свой драйвер (psycopg, не asyncpg,
# на котором работает SQLAlchemy/тикеты). Два разных клиента к одной базе
# для двух разных забот (см. app/agent/graph/checkpointer.py).
langgraph-checkpoint-postgres==3.1.0
psycopg[binary,pool]==3.3.4

# --- RAG (опционально). По умолчанию используем локальный ретривер без внешних
#     зависимостей. Раскомментируйте, если поднимаете Qdrant через docker compose. ---
# qdrant-client==1.11.2

# --- Инструменты (занятие 3) ---
# 10 инструментов и защита от инъекций (sanitizer, SSRF-guard) написаны на
# stdlib + httpx (уже выше) — новых внешних зависимостей не потребовалось.
# Это тоже часть урока: не каждая новая возможность агента требует новый пакет.

# --- Игровой режим (GAME_MODE=true) ---
# Приём файлов multipart-формой (FastAPI требует явную зависимость).
python-multipart==0.0.12
# Извлечение текста из PDF для инструмента read_attached_file. Чистый Python,
# без системных библиотек — не тянет за собой ничего в образ.
pypdf==5.1.0

# --- Тесты ---
pytest==8.3.3
pytest-asyncio==0.24.0
